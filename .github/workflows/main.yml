name: Deploy to Alwaysdata

on:
  push:
    branches:
      - main

jobs:

  deploy-api:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to APIs to AlwaysData (apiliverpool.alwaysdata.net)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp-apiliverpool.alwaysdata.net
          username: ${{ secrets.APILIVERPOOL_AD_USER }}
          password: ${{ secrets.APILIVERPOOL_AD_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./
          server-dir: /www/
          security: loose
